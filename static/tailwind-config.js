// Tailwind CSS Configuration
tailwind.config = {
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        // Custom colors can be added here if needed
      },
    },
  },
  plugins: [],
};
